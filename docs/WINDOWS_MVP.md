# Windows 基础版

## 范围

Windows 优先，其他平台保留为后续目标。仓库使用单一根 Cargo workspace，只包含 GPUI 应用、共享核心和 Windows 平台实现。

首批功能：文本采集、历史持久化、精确去重、搜索、复制、删除、置顶、暂停/恢复。后续已增加全局唤出快捷键、HTML/RTF、PNG 图片和普通文件路径的采集、历史、预览与复制，以及经全局快捷键唤出后返回原窗口的受控粘贴；虚拟文件保真仍待开发。

## 当前实现

- `crates/clipboard-core`：包含数据库 schema、迁移、仓储和去重源码，提供 UI 无关的文本、HTML/RTF、图片与文件路径历史用例。
- `crates/clipboard-platform`：Windows 原生剪贴板事件监听及全局快捷键；独立 worker 串行处理历史操作；请求代次、实例锁、重复启动唤出、有界队列和关闭清理。
- `crates/clipboard-gpui`：Windows 原生主窗口、搜索、虚拟历史列表、复制/删除/置顶、暂停确认、加载更多、文本/富文本/图片预览与文件详情、收藏与收藏筛选、导入分组浏览、组内拖拽排序、系统托盘和错误反馈。

数据库与去重源码已经物理迁入 `clipboard-core`；核心不依赖桌面 UI 框架或 Windows API。

默认目录由 `ProjectDirs::from("com", "ASLant", "ElegantClipboard-GPUI")` 生成，位于 Windows 用户 LocalAppData。不自动读取旧版安装目录中的数据库；可通过显式导入命令复制旧库到空 GPUI 数据目录。自动测试使用临时目录、合成文本和仓库内 PNG 样本。

限制：单条文本最多 1 MiB，富文本的 HTML、RTF 原始字节与纯文本合计最多 1 MiB；图片最多 2500 万像素、编码 PNG 最多 50 MiB；文件路径列表最多 256 项或 1 MiB，仅保存原路径，不备份文件内容。普通未固定历史按原仓储规则保留一万条；空白文本不保存。重复的完全相同文本移动到顶部，文字和换行原样保存。队列满时报告采集失败，不把不同复制事件主动合并成最后一条。

## 依赖策略

2026-09-18 已查询原有直接依赖的 crates.io 元数据；新增 base64 于 2026-09-19 核对：

| 直接依赖 | 当前使用版本 |
|---|---|
| gpui-kit | 0.6.4 |
| tray-icon | 0.25.1 |
| raw-window-handle | 0.6.2 |
| rusqlite | 0.40.2 |
| clipboard-rs | 0.3.5（注册表版，启用 `image` 功能） |
| clipboard-win | 5.4.1（Windows 富文本多格式写回） |
| windows | 0.62.2 |
| anyhow | 1.0.104 |
| async-channel | 2.5.0 |
| blake3 | 1.8.7 |
| base64 | 0.23.1 |
| directories | 6.0.0 |
| parking_lot | 0.12.5 |
| serde | 1.0.229 |
| serde_json | 1.0.151 |
| tracing | 0.1.44 |
| chrono（卡片绝对/相对时间） | 0.4.45 |
| image（来源图标 PNG 编码） | 0.25.10 |
| tempfile（测试及来源图标原子缓存） | 3.27.0 |

工具链要求 Rust 1.98+；本机已使用 1.98.0。Cargo.lock 固定实际解析结果。依赖升级流程：查询最新稳定版本 → 更新 workspace requirements → `cargo update` → fmt/clippy/test/build → 原生交互验收。存在上游兼容约束的间接依赖不能强行替换为不同主版本。

**上游例外**：本轮继续接入最新稳定 gpui-kit，其 Windows 依赖路径 `gpui-kit → gpui-base/gpui-component → ropey` 使用 ropey 2.0.0-beta.1。详见 [技术决策](decisions/0001-windows-gpui.md)，不宣称整个依赖树均为稳定版。

## 启动与操作

```powershell
# 正常使用：启动原生窗口并记录后续复制的受支持内容
cargo run -p elegant-clipboard-gpui --locked

# 隔离检查：禁用采集，只浏览指定目录中的历史
cargo run -p elegant-clipboard-gpui --locked -- --no-monitor --data-dir .\target\gpui-check

# 窗口冒烟：禁用采集，约 3 秒后自动退出
cargo run -p elegant-clipboard-gpui --locked -- --smoke-test --data-dir .\target\gpui-smoke

# 从旧版数据库导入到尚无 clipboard.db 的 GPUI 数据目录；命令完成后退出
cargo run -p elegant-clipboard-gpui --locked -- --import-db "C:\旧版目录\clipboard.db"
```

- 搜索支持中文与字面 `%`/`_`，等待 150ms 后查询；回车复制当前选中结果。卡片摘要中出现的搜索词会高亮显示；若词只存在于文本正文较深的位置，卡片改为显示第一处命中附近的片段并高亮。匹配按 Unicode 字符逐个比较且不区分大小写；文件路径等没有正文的记录继续使用原摘要。
- 列表支持 ↑/↓ 逐条选择、PageUp/PageDown 翻页选择、Home/End 跳到首尾、←/→ 按“全部/收藏/文本/其他”顺序切换分类、Ctrl+←/→ 切换默认及自定义分组并滚动到选中分组、Enter 按卡片点击规则复制或粘贴、Ctrl+Enter 明确粘贴到原窗口、Delete 删除；Ctrl+F 聚焦搜索。
- 搜索框按 ↓ 会聚焦列表首条已加载记录；列表首条按 ↑ 会返回搜索框。搜索框中的其他方向键继续用于编辑文本，空结果或查询进行中不会转移焦点。
- 当前已加载历史超过 8 条时，列表上方的固定信息栏提供“返回顶部”，点击后严格滚动到首条；该入口不会覆盖卡片操作区。
- 点击卡片非按钮区域时，若通过全局快捷键保留了有效的原窗口目标，就使用现有受控复制后粘贴流程；否则复制该记录供手动粘贴。进入批量选择模式后，卡片点击改为选择或取消选择，Shift 点击可在当前已加载顺序中连选；拖拽和单条记录操作按钮在该模式中关闭。普通模式下预览、收藏、置顶、分组及复制等卡片按钮保持各自独立动作。
- 普通模式下仅卡片左右两侧的 32px 区域可启动拖动排序，卡片中间保留点击动作。显示设置中的“显示卡片拖动区域”默认开启；关闭后仅隐藏两侧提示，拖动仍有效。批量模式不显示拖动区域，也不能拖动排序。
- 普通模式下双击文本、网址或富文本卡片可直接进入编辑；第一次单击会等待系统双击间隔结束后才复制或粘贴，以免双击先执行粘贴。批量模式、键盘操作及图片/文件卡片仍立即响应。搜索、筛选、拖动、打开预览或隐藏窗口会取消尚未执行的单击。
- 非批量模式下右键卡片会显示按记录类型变化的菜单：复制、返回原窗口粘贴、纯文本或路径操作、查看详情、文本编辑、收藏、置顶、移动分组和删除；图片及文件还可在资源管理器中定位或另存为。编辑会先读取该记录完整内容，再进入已有编辑界面；目标窗口失效、记录不再可见等情况仍由执行时检查处理。
- Esc 优先取消拖动或关闭当前确认、分组编辑与移动面板，再退出批量选择模式；没有待处理界面时隐藏历史窗口到托盘。托盘不可用时按关闭窗口流程退出；复制后粘贴正在处理时不会用 Esc 隐藏窗口。全文预览中的 Esc 先取消编辑或返回列表。
- 列表按 Shift+Enter 会使用选中记录已保存的纯文本表示；从有效目标窗口唤出时写入纯文本后尝试返回原窗口粘贴，否则只复制供手动粘贴。富文本卡片和预览中的纯文本按钮沿用相同规则；图片和文件记录、没有纯文本表示的富文本记录会明确报错。
- 置顶、删除与复制按钮直接调用后台服务；文本复制使用完整正文，HTML/RTF 在同一次剪贴板操作中逐项写入纯文本及可用的富格式，任一格式写入或读回校验失败会明确报错，卡片“纯文本”与预览“复制纯文本”只写入其已保存的纯文本表示。没有纯文本表示时明确报错，不复制预览占位文案；图片从保存的文件解码写回剪贴板，文件路径在源文件仍存在时写回剪贴板。文件卡片与文件/图片预览可将有效路径复制为换行分隔的纯文本，也可在资源管理器中定位第一项；从有效目标窗口唤出时“复制路径”变为受控“粘贴路径”。
- “暂停记录 / 恢复记录”状态保存在 GPUI 数据库，重启后先恢复状态再启动监听；保存成功后界面才切换。暂停时已有历史仍可搜索和复制。
- 空数据目录首次打开时显示四步原生引导，依次说明采集、搜索、置顶收藏和键盘快捷键；“下一步”“开始使用”“跳过”及 Esc 可操作，完成状态写入 SQLite 后才关闭引导，重启不再出现。已有历史的 GPUI 数据目录会自动标记为已完成，避免升级后遮挡历史；窗口冒烟模式不显示引导。引导中有关自动粘贴的说明以从目标应用用全局快捷键唤出为前提。
- 设置窗口按常规、显示、外观、数据、应用过滤、音效、快捷键和关于分页导航；现有设置卡片归入相应页面，切换页面时结束未完成的快捷键录制。导航标签随界面语言切换。
- 音效页可分别启停历史复制和受控粘贴的短音，并选择“立即”或“成功后”播放；试听不受开关限制。复制音效覆盖普通、纯文本、路径和批量合并复制；粘贴音效在向原窗口发送快捷键时触发，“成功后”仅表示 Windows 接受按键注入，无法证明目标应用已完成粘贴。设置保存在 SQLite 并随 GPUI 备份恢复；音频设备不可用时不会阻止复制或粘贴。
- 关于页显示应用版本、作者，并提供作者主页、项目仓库和问题反馈入口；使用 Windows 默认浏览器打开固定的 HTTPS 链接，打开失败时在设置页显示错误。
- 设置窗口的“监听内容类型”可分别切换文本、网址、HTML、RTF、图片和文件的自动采集，至少保留一种。富文本含多个格式时保留已启用的格式；富格式均停用而纯文本表示已启用时回退为文本。设置保存在 SQLite 并随 GPUI ZIP 备份恢复。手动复制历史记录及导入不受此设置限制。
- 设置窗口的“来源应用过滤”支持启停、黑名单/白名单、手工添加或删除规则，并可从当前可见窗口的运行中应用列表选择进程。规则按来源应用名、进程名及完整路径匹配，普通规则不区分大小写地匹配子串，`*` 与 `?` 通配符按整个名称或路径匹配。来源识别失败或规则列表为空时仍记录；过滤仅作用于后续自动采集，不删除已有历史。规则保存在 SQLite 并随 GPUI ZIP 备份恢复。
- 点击“查看”或在列表按 Space 打开完整文本、HTML/RTF 的纯文本表示、图片或文件详情；文本可滚动、选取，图片按原比例显示。图片预览可用缩放按钮或 Ctrl+滚轮按 10% 调整，范围为 50% 至 400%；点击百分比可恢复 100%，放大后可滚动查看，切换预览时重置缩放。文件详情按原顺序显示名称、原始路径、文件/文件夹、普通文件大小与失效状态；旧备份恢复的源文件丢失但受管暂存副本可用时，同时显示备份路径。元数据在后台读取，详情列表独立滚动。富文本预览同时提供“复制纯文本”和“复制富文本”，窄窗口中按钮可换行。图片和普通文件可从预览打开 Windows 原生保存对话框并另存第一项，默认沿用源文件名；后台在复制前重新解析记录，只接受现存普通文件，拒绝文件夹、失效源、相对目标和原地覆盖。文本、网址与富文本可从预览进入编辑，保存后保留记录 ID、分组与收藏状态并刷新搜索结果；修改富文本会转为纯文本，空内容保存会报错，请使用独立“删除”按钮。Esc 在编辑时取消修改，在只读预览时返回列表。预览按需从后台读取，关闭后的旧响应不会重新打开预览。
- 单张图片文件在历史卡片、完整文件详情及文件悬浮浮窗中显示图片，保留文件名、原始路径和大小信息；多文件及不支持预览的记录继续显示文件详情或路径。卡片缩略图的外部文件元数据检查在独立后台线程执行，不阻塞列表绘制；同一批已加载记录不会在每次列表刷新时重复检查。仅预览确认存在、元数据可读且大小已知的 PNG/JPEG/GIF/WebP/BMP 文件；本地路径上限 50 MiB，UNC 路径上限 10 MiB，超限或图片解码失败时显示回退内容。受管备份副本仍只在完整文件详情和悬浮浮窗中预览，历史卡片继续显示原始路径。
- 文件卡片还会在后台检查所有原始路径。任一原始路径不存在时，列表标题显示红色“源文件已失效”；无法读取元数据或路径无效时显示“文件无法读取”。文件名也使用警示色，图片缩略图仅在单文件可读且大小合规时显示。该提示只描述原始来源；旧备份中的受管副本是否可用于复制，仍以完整文件详情和执行时的校验为准。检查结果按记录 ID 和路径内容缓存，记录离开当前列表或路径变化后重新检查；本地路径先完成，网络路径随后返回，避免网络检查拖延本地提示。
- 文件卡片使用警告、普通文件和文件夹图标区分失效、单文件、文件夹与多文件；图片文件符合预览限制时优先显示缩略图。超过图片预览上限的文件显示“图片过大”，保留普通文件图标和文件名。多文件卡片显示前 3 个文件名，更多项以省略号结尾；显示大小时使用后台读取的真实文件字节数，只有全部路径都是可读普通文件时才显示总量，文件夹或失效来源不再显示路径字符串的字节数。
- 点击条目“收藏 / 取消收藏”保存常用记录；分类栏可切换“全部 / 收藏 / 文本 / 其他”，其中“文本”包含 text、HTML、RTF，“其他”包含图片、文件和 URL。分类与自定义分组互斥，搜索、数量与加载更多均由后台按当前分类查询，不会只筛选已加载记录；取消收藏不会删除记录。收藏状态持久化，启动默认显示全部。
- 自定义分组会在筛选栏下方显示，也可点击“＋ 新建”输入名称后创建。自定义分组可拖动排序，左右半区决定插入位置；拖至分组栏边缘时自动横向滚动，以访问未显示的目标。顺序保存到 SQLite，默认分组固定在首位。点击分组切换其历史，搜索、收藏筛选、数量与分页均限定在当前分组；选中自定义分组后可重命名或删除。删除前显示名称和记录数，确认后将组内记录移到默认分组，保留收藏与原始内容；与旧版级联删除记录的行为不同。分组栏在窄窗口中横向滚动。每张卡片的“分组”按钮可展开目标选择栏，将已有记录移至自定义分组或移回默认分组；保存后当前列表与组内数量更新。新采集内容仍进入默认分组。
- 文本、网址、富文本、图片和文件记录均可从卡片左右两侧拖动区域拖动；拖动浮层显示类型和摘要，目标上/下半区的主题色插入线表示可放置，红色插入线表示当前列表状态已失效。按钮区保留各自点击操作；指针停在列表边缘时会持续滚动已加载记录，并随虚拟列表推进更新落点。卡片与分组共用边缘区和基础滚动节拍。顺序写入 SQLite，收藏列表维护独立顺序。拖到置顶卡片前后会自动置顶，拖到普通卡片前后会自动取消置顶，状态与目标区顺序在同一事务中保存和回滚。Esc 可取消拖动。各种记录使用统一的预览容器、操作区和行高；浮层、插入线及排序反馈共用动效时长并遵从系统减少动态效果设置。排序已写入但列表刷新失败时返回明确错误并解除操作锁定，重启后可重新查看顺序。
- 默认加载 100 条，点击“加载更多”继续；仅渲染可见行。
- 设置窗口中的“外观”提供跟随系统、浅色和深色；保存成功后生效，重启恢复选择。设置存于同一 SQLite 数据库，手动主题不随系统通知改变；未知设置值回退为跟随系统。
- 窗口使用 gpui-kit `TitleBar` 自定义标题栏，列表与全文预览共用；支持拖动、双击最大化/还原、最小化、边缘缩放和关闭。
- 设置窗口的“记住窗口大小”默认开启：主窗口停止缩放 300 ms 后保存最后一次普通窗口的内容区逻辑尺寸，重启时恢复；关闭后清除已保存尺寸并停止写入，重新开启后保存当前普通窗口尺寸。最大化和全屏状态不会覆盖普通尺寸；保存值缺失、损坏或超出 420×520 至 4096×2160 时回退到 560×760。设置窗口可选择唤出位置：默认跟随光标，优先出现在光标右下方，水平空间不足时翻转到左侧；也可在光标所在显示器工作区居中，或在当前会话保持上次位置。前两种模式在启动及托盘、快捷键唤出时将窗口限制在光标所在显示器的工作区内，最大化或全屏时不移动。模式保存在 SQLite；窗口坐标不跨重启持久化，避免显示器变化后落到屏幕外。窗口尺寸记忆开关、已保存尺寸和定位模式随 GPUI ZIP 备份导出和恢复。
- 设置窗口的“隐藏时重置搜索、筛选和滚动”默认关闭。开启后，主窗口从关闭、Esc、快捷键、托盘或外部点击隐藏时，会清空搜索、回到全部记录和默认分组，并将列表滚动到顶部。无论开关状态，隐藏时都会退出未提交的批量选择；批量操作或粘贴请求正在处理时暂不隐藏窗口。预览文本正在编辑或保存时保留预览，避免丢失未保存内容。该设置随 GPUI ZIP 备份导出和恢复。
- 设置窗口的“唤出时清空搜索”默认开启，主窗口从托盘、快捷键或重复启动唤回时清空已有搜索并刷新当前分类/分组结果；“唤出时聚焦搜索”默认关闭，开启后启动显示及每次唤出时将键盘焦点放到搜索框。两项设置保存在 SQLite，并随 GPUI ZIP 备份导出和恢复。
- 主窗口顶部工具栏提供清理历史、批量选择、置顶和设置按钮；设置按钮打开独立设置窗口，已打开时将其激活，托盘菜单也可打开设置。工具栏可在设置窗口通过拖动手柄或上移/下移按钮调整顺序，并调整显隐，设置按钮始终显示，配置保存在 SQLite 并随 GPUI 备份恢复。设置窗口的“列表显示”可隐藏分类筛选栏（隐藏时回到全部记录，并停止分类左右键切换），或在紧凑、标准、宽松三档卡片密度间切换；卡片预览行数可独立设置为 1 至 10 行；时间、字符数、大小和来源应用信息可分别显示或隐藏；显示时间可选绝对或相对格式。剪贴板变化时优先按剪贴板所有者识别来源进程，失败时回退到前台窗口；无法读取进程信息时不显示来源。识别到的可执行文件图标在后台提取并缓存为 PNG，卡片可按名称、图标或两者显示，提取失败时回退为名称；被记录引用的图标随 GPUI 备份导出和恢复。卡片行高随密度和预览行数变化，翻页步长及拖拽反馈距离同步更新；显示设置持久化并随备份恢复。置顶使用 Windows topmost 窗口层级，开启后按钮显示选中状态，受控粘贴时窗口保持可见但键盘焦点仍交回目标应用；未置顶时可在“窗口行为”中选择粘贴后是否隐藏，默认隐藏；选择保持可见时仍将键盘焦点交给目标窗口。还可控制粘贴后是否将该条普通记录移到所在列表首位，默认开启；已置顶记录不改变顺序，合并粘贴不触发单条排序。仅在向原窗口成功发送粘贴快捷键后调整顺序。窗口可见且未置顶时，点击应用外的窗口或桌面会隐藏历史窗口到托盘；应用自己的悬停浮窗及设置窗口不触发隐藏，设置窗口打开或粘贴请求处理中也不自动隐藏。监听通过 Windows 鼠标钩子发送有界的点击事件，仅在主窗口可见时转发；不保存点击位置。置顶仅作用于当前运行会话，关闭应用后不保留。主工具栏按钮在窄窗口中可换行，避免越界或重叠。
- 关闭窗口后隐藏到系统托盘，左键托盘图标和全局快捷键可切换窗口显隐，最小化时会恢复；右键菜单“打开剪贴板历史”始终显示并聚焦窗口，粘贴请求处理中暂不隐藏。右键菜单“退出 ElegantClipboard”结束后台监听。在设置窗口中，“唤出”可选 `Ctrl+Shift+V`（默认）、`Alt+C`、`Ctrl+Alt+V` 或关闭，选择保存在 GPUI 设置中。组合键被占用时提示并恢复原快捷键，托盘入口仍可用；托盘初始化失败时关闭窗口正常退出。用全局快捷键从目标应用唤出后，卡片与预览上的“粘贴”按钮可先复制，再隐藏历史窗口并尝试向原窗口发送设置中选择的 Ctrl+V 或 Shift+Insert；离开历史窗口会清除原目标和未完成的粘贴请求，再直接点回时需从目标应用重新按热键唤出。托盘唤出或目标窗口失效时仅能手动复制。焦点不匹配、修饰键未松开、复制后剪贴板序列号变化或按键注入失败会显示错误并保留已复制的内容。Windows 无法从按键注入返回值确认目标应用已经完成粘贴，因此成功提示仅表示已发送快捷键。粘贴按键可选 Ctrl+V（默认）或 Shift+Insert，配置保存在 SQLite 并随 GPUI 备份恢复；当前不支持任意按键组合录制。
- 设置窗口中的“开机启动”默认关闭；用户开启后在当前用户 Run 键写入与该数据目录对应的独立项，关闭后删除。启动命令使用当前可执行文件、该数据目录和 `--start-hidden`；开机启动时窗口不弹出，可由托盘或快捷键唤回，托盘初始化失败则显示窗口。移动可执行文件后需要在新位置重新开启，才能更新启动命令。
- Windows MSVC 版的 GPUI 主线程预留 8 MiB 栈空间；设置窗口元素树在系统默认 1 MiB 预留量下会触发栈溢出。该数值是虚拟地址预留，物理内存随实际使用提交；构建脚本仅对本应用的 Windows MSVC 可执行文件设置链接参数。隔离窗口打开设置和编辑快捷键的过程见 [快捷键界面验收](evidence/windows-quick-paste-shortcuts-2026-09-25.md)。
- 设置窗口中的“快速粘贴快捷键”默认开启。应用正常监听剪贴板时，Alt+1～Alt+0（数字行或数字小键盘）对应当前分组前十条记录，Ctrl+Alt+1～3 对应当前分组前三条收藏记录；快捷键在外部应用触发后，后台解析槽位、复制记录并沿用受控粘贴的目标窗口、剪贴板序号及修饰键检查。槽位为空会报告错误，部分快捷键注册失败会提示仍可使用的组合；`--no-monitor` 和窗口冒烟模式不注册这些全局快捷键。设置窗口可展开普通与收藏两组各 10 个槽位，逐项录制或输入 Ctrl/Alt/Shift 与数字、字母、F1～F24 或常用导航键的组合，也可逐项停用或恢复默认，或按组一次停用全部及恢复全部默认；至少需要 Ctrl 或 Alt，不接受 Win、重复组合或与唤出快捷键冲突的组合。新组合注册失败时回退到旧配置；设置保存在 SQLite 并随 GPUI ZIP 备份恢复。按键录制在设置窗口焦点区域完成，Esc 可取消；文本输入可作为录制受键盘布局或系统拦截影响时的备用方式。实际跨应用按键注入和快捷键冲突仍需真实桌面验收。
- 打开设置窗口会在后台统计当前 GPUI 数据占用，分别显示 SQLite 主文件/WAL/SHM、受管图片和暂存载荷的文件数与大小；“刷新”重新读取磁盘，“打开目录”用系统资源管理器打开当前实例实际使用的数据目录。统计跳过符号链接和目录外无关文件，读取失败会显示错误而不返回低估结果。“整理数据库”在后台执行 SQLite optimize、VACUUM 和 WAL 截断，回收已删除记录留下的空闲页，完成后刷新占用统计；整理不删除仍存在的历史或设置。
- 同一数据目录下再次启动程序会唤回已有窗口并退出第二个进程；指定另一数据目录可并行运行隔离实例。不同实例之间全局快捷键仍只能由一个实例注册，冲突会在另一个窗口提示。
- 导入只读访问旧库，使用 SQLite 在线备份获取含 WAL 的一致快照，在新目录升级副本并校验；如果 GPUI 目录已有 `clipboard.db` 就拒绝覆盖。旧版默认数据库位于旧程序可执行文件旁；如旧版 `config.json` 配置了 `data_path`，应指定该目录中的 `clipboard.db`。可用 `--data-dir` 为导入选择另一个空目录。当前界面可浏览默认分组及自定义分组的文本、网址、HTML/RTF、图片和普通文件路径；导入的外部图片与文件保留原路径，导入不会复制原文件。文件记录若仅含旧版虚拟文件载荷而没有可用原路径，不能在新版本复制。不要把开发版的 `--data-dir` 指向旧版数据目录。
- 主窗口“导出备份”保存 v2 GPUI ZIP：在线 SQLite 快照、引用的可读取图片、图标及暂存文件。默认排除非 GPUI 设置（包括旧库潜在的密码或 token）；源资产缺失时显示数量，记录保留原路径。备份不包含普通文件记录指向的原文件，但包含可读取的暂存副本。恢复使用 `elegant-clipboard-gpui --import-backup <备份.zip> --data-dir <空目录>`，兼容 v1/v2；恢复时同样筛除归档中的非 GPUI 设置。在创建暂存文件前拒绝重复归档路径、超过 10 万项或展开后超过 50 GiB 的备份，随后验证格式、数据库与资产引用，并在安装或打包暂存数据库前完成 WAL checkpoint；检查失败则不安装。不覆盖已有数据库或资产目录。恢复期间目标目录不能由其他实例使用。
- 旧版 Tauri ZIP 可用 `elegant-clipboard-gpui --import-legacy-backup <旧版备份.zip> --data-dir <空目录>` 导入；它会升级数据库，恢复并重建 `images/`、`icons/`、`staged/` 路径。归档中未包含的外部图片保留原引用并计入报告。原文件丢失时，普通文件记录可使用恢复到 GPUI 目录中的暂存副本进行复制；删除或自动淘汰记录后会清理无引用的暂存副本。旧版虚拟文件的额外剪贴板格式仍不支持写回。单独的旧 `clipboard.db` 仍用 `--import-db`，该方式不会复制外部媒体。两种旧数据导入都只将 GPUI 使用的外观、快捷键和暂停采集设置写入新数据库，旧版专属设置及凭据保留在原来源中。
- 分组栏“清理历史”在明确确认后删除当前分组中未置顶且未收藏的记录；搜索、收藏或类型筛选不缩小清理范围。其他分组及受保护记录保留，本地受管理图片在无引用时删除。操作无法撤销，可先使用“导出备份”。
- 设置窗口的“删除全部历史”另有强确认，删除所有分组中的全部记录，包括置顶和收藏，同时清理无引用的受管图片及暂存副本；主题、快捷键、暂停状态等设置和自定义分组保留。完成后关闭已删除记录的预览并清空批量选择。此操作无法撤销，可先导出备份。
- 分类栏“批量选择”可在尚未选择记录时进入批量模式；卡片标题区“选择”或列表中的 Ctrl+A 也会进入该模式。模式内点击整张卡片可切换选择，按住 Shift 点击可按当前已加载顺序连选两者之间的记录，并保留此前选择；取消最后一条选择后仍保持批量模式。“取消选择”仅清空所选记录，“退出批量”或 Esc 退出模式。批量模式下单条记录的 Enter、Shift+Enter、Ctrl+Enter、Space 和 Delete 快捷键不执行，避免误操作焦点记录。选择至少两条后可按当前列表顺序“合并复制”；文本表示以换行连接，文件和受管图片同时作为去重后的文件路径列表写入，并逐项读回校验。由有效目标窗口热键唤出时按钮变为“合并粘贴”，沿用单条受控粘贴的焦点、修饰键和剪贴板序号检查；目标失效或用户离开窗口后仅保留已复制内容。“删除选中”需再次确认，并会删除所选的置顶和收藏记录。选择仅作用于当前已加载列表，切换搜索、分组或收藏视图会退出批量模式；后台校验列表代次和分组，在同一事务内删除，失败不部分删除，提交后按引用清理受管图片及暂存副本。

- 鼠标停留在历史卡片上达到设置的延时后，会单独请求完整内容并在鼠标所在屏幕内打开悬停预览。图片默认开启；文本、富文本和文件路径可在设置中分别启用，默认关闭。设置还可选择 250/500/750/1000ms 延时、自动/左侧/右侧位置、5%/10%/20%/50% 的图片缩放步进，以及默认关闭的“大图使用更大浮窗”；开启后读取历史中保存的图片尺寸，将图片浮窗扩大但限制在当前显示器内，尺寸缺失或无效时保持默认 460×300。设置保存后重启与备份恢复均保留。文本和文件路径可在只读浮窗中滚动查看；图片浮窗支持 Ctrl+滚轮在 50% 至 400% 间缩放。鼠标离开卡片和浮窗后延时关闭；搜索、筛选、批量模式、拖动、点击完整预览和隐藏主窗口会关闭悬停预览。悬停请求与点击预览使用独立响应，过期响应不会重新打开浮窗。图片浮窗大小以及文本、文件悬浮内容与离开后的关闭行为已用隔离数据在桌面窗口验收；跨显示器边界仍需验收。

- 图片和单张图片文件的悬停浮窗提供缩小、百分比重置及放大按钮；缩放范围为 50% 至 400%，按钮使用设置的缩放步进，Ctrl+滚轮仍可使用。隔离窗口已验证图片文件浮窗中 100%→110%→100%→90% 的控件交互，普通图片浮窗也显示同一组控件。

- 常规设置可启用“清理历史免确认”，默认关闭；开启后工具栏“清理历史”直接请求清理当前分组未置顶且未收藏的记录。搜索和筛选仍不缩小清理范围。“清空所有记录”和批量删除继续要求确认。设置持久化并随 GPUI 备份保留。

## 验证命令

在仓库根目录运行：

```powershell
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo fmt --all -- --check
cargo build -p elegant-clipboard-gpui --locked
cargo run -p clipboard-platform --example isolated_clipboard_smoke --locked
.\scripts\package-gpui-windows.ps1
```

打包脚本仅接受 Windows x64 MSVC 构建，使用锁定依赖生成 `target/packages/elegant-clipboard-gpui-v<版本>-windows-x64.zip` 与 `.zip.sha256`。压缩包含 exe、许可证和运行说明，程序默认在 LocalAppData 保存数据；这是未经签名的独立压缩包，不是安装器或自动更新制品。

当前 231 项测试通过（核心 150、Windows 后端 55、应用状态/参数 26），另有 2 项需要系统程序图标的测试默认跳过，覆盖暂停确认与重启恢复、窗口尺寸校验/持久化/备份恢复、搜索代次与选中 ID 保持、类型分类的搜索、数量及后台快照、摘要搜索高亮的 UTF-8 边界与正文命中片段、自定义分组浏览、创建、重命名、安全删除与拖拽排序、跨置顶区状态切换与事务回滚、历史卡片持续边缘滚动边界、全局清空、批量删除的事务回滚与媒体清理、按列表顺序合并文本和去重文件路径、文件/图片路径操作、安全的资源管理器参数、另存为边界、数据占用统计与数据库空间回收、记录移动与组内排序、文本编辑与过期内容拒绝、HTML/RTF 二进制存储与混排拖动及纯文本复制判定、图片存储、文本/图片/文件路径预览、容量淘汰清理、文件源缺失、历史损坏行显式报错及媒体引用查询失败时停止清理、后台采集错误与设置确认隔离、命令失败仅回收对应操作状态、键盘边界、冒烟参数隔离、监听类型与来源应用过滤的持久化、采集及备份恢复，以及在线旧库导入、GPUI v1/v2 备份导出恢复、网址兼容、拒绝无效来源、实例唤出和快捷键设置。fmt、Clippy（warnings 视为错误）、Windows debug/release 构建及窗口冒烟通过；分组栏、卡片两侧拖动和跨置顶区拖放已在隔离窗口验收，分类栏、搜索高亮和三档卡片密度已在合成数据的隔离窗口完成鼠标验收；工具栏拖动已在隔离设置窗口验收。真实剪贴板采集和最小窗口边界不在这些验收范围内。

Windows 常驻资源可用 `scripts/measure-gpui-idle.ps1 -ProcessId <GPUI 进程 ID> -OutputPath <CSV 路径>` 采样，默认持续 8 小时、每分钟记录私有内存、工作集、句柄、线程和累计 CPU 时间。脚本只观察进程，不启动或结束应用；执行前应使用独立数据目录并关闭采集与全局快捷键。采样通过不等于完成休眠唤醒和跨应用粘贴验收。

原生窗口已通过启动/退出、中文路径、重复启动唤回，以及 120 条合成数据下的加载更多、字面搜索、空结果、置顶和删除检查；10,000 条合成文本的启动与末尾搜索也已验收。HTML/RTF、图片、文件卡片和对应预览均在隔离目录中截图核验；文件详情另覆盖有效文件、文件夹、受管备份副本、失效路径与最小窗口滚动布局。普通窗口缩放、最大化不覆盖、重启恢复和损坏尺寸回退已在真实窗口验收。富文本卡片之间的真实鼠标拖动也已核对 SQLite 顺序。搜索框、分组创建和正文编辑已在活动 Windows 桌面通过拼音组合输入及候选确认验收。其他窗口交互使用隔离目录并禁用实际采集；图片另存为已打开原生保存对话框并取消，数据占用统计、数据库空间回收、全局清空历史和最小窗口布局也已核对；另在非交互式窗口站中完成文本、HTML/RTF、图片和文件路径的真实系统剪贴板采集与写回，交互桌面的剪贴板序列号未变。Windows x64 独立压缩包已完成 SHA-256 核对和解压运行冒烟。仍未覆盖常见应用之间的真实复制粘贴、其他输入法及输入场景、长时间运行、安装升级或跨平台运行。完整过程见 [验证记录](evidence/windows-ui-2026-09-18.md)、[规模验证](evidence/windows-scale-2026-09-19.md)、[中文输入法验收](evidence/windows-ime-2026-09-19.md)、[隔离剪贴板记录](evidence/windows-private-clipboard-2026-09-19.md)、[文件详情记录](evidence/windows-file-details-2026-09-19.md)、[另存为记录](evidence/windows-save-as-2026-09-19.md)、[数据占用记录](evidence/windows-data-size-2026-09-19.md)、[数据库整理记录](evidence/windows-database-maintenance-2026-09-19.md)、[全局清空记录](evidence/windows-clear-all-history-2026-09-19.md)、[窗口尺寸记录](evidence/windows-window-size-2026-09-19.md)和[独立包验收](evidence/windows-package-2026-09-19.md)。

Windows CI 包含 fmt、Clippy、locked 测试和应用构建，尚未在远端运行。CI 使用已核验的 [checkout v7.0.1](https://github.com/actions/checkout/releases/tag/v7.0.1) 与 [rust-cache v2.9.2](https://github.com/Swatinem/rust-cache/releases/tag/v2.9.2)。

收藏增量验证见 [2026-09-19 验证记录](evidence/windows-favorites-2026-09-19.md)。

主题增量验证见 [2026-09-19 主题验证记录](evidence/windows-theme-2026-09-19.md)。

界面间距与动画规则见 [UI 规范](UI_GUIDELINES.md)，历史卡片拖拽验证见 [2026-09-19 排序验证记录](evidence/windows-reorder-2026-09-19.md)与[持续边缘滚动记录](evidence/windows-card-edge-scroll-2026-09-19.md)，分组拖拽验证见 [2026-09-19 分组排序记录](evidence/windows-group-reorder-2026-09-19.md)。

跨置顶区拖放验证见 [2026-09-19 跨区拖放记录](evidence/windows-cross-pin-drag-2026-09-19.md)。

窗口置顶验证见 [2026-09-19 窗口置顶记录](evidence/windows-window-pin-2026-09-19.md)。

窗口尺寸恢复验证见 [2026-09-19 窗口尺寸记录](evidence/windows-window-size-2026-09-19.md)。

托盘验证见 [2026-09-19 托盘记录](evidence/windows-tray-2026-09-19.md)。

全局快捷键验证见 [2026-09-19 快捷键记录](evidence/windows-hotkey-2026-09-19.md)。

旧数据导入验证见 [2026-09-19 导入记录](evidence/windows-import-2026-09-19.md)。

重复启动验收见 [2026-09-19 实例唤出记录](evidence/windows-instance-2026-09-19.md)。

可配置快捷键验收见 [2026-09-19 快捷键设置记录](evidence/windows-hotkey-settings-2026-09-19.md)。

暂停状态持久化验收见 [2026-09-19 暂停记录](evidence/windows-pause-2026-09-19.md)。

图片存储、工作线程及界面验收分别见 [核心](evidence/windows-image-core-2026-09-19.md)、[后端](evidence/windows-image-platform-2026-09-19.md)和[界面](evidence/windows-image-ui-2026-09-19.md)。

普通文件路径接入验证见 [文件记录](evidence/windows-files-2026-09-19.md)。

HTML/RTF 接入验证见 [富文本记录](evidence/windows-rich-text-2026-09-19.md)。

受控粘贴实现与验证边界见 [粘贴记录](evidence/windows-auto-paste-2026-09-19.md)，原窗口生命周期验收见 [目标失效记录](evidence/windows-paste-target-lifecycle-2026-09-19.md)。

粘贴后窗口显隐、排序设置和相关显示控件的隔离桌面验收见 [2026-09-25 粘贴行为记录](evidence/windows-paste-behavior-2026-09-25.md)。
自动粘贴按键切换及重启持久化的隔离桌面验收见 [2026-09-25 粘贴按键记录](evidence/windows-paste-key-2026-09-25.md)。

导入分组浏览与排序见 [分组记录](evidence/windows-groups-2026-09-19.md)。

新建分组见 [分组创建记录](evidence/windows-group-create-2026-09-19.md)。

记录移入分组见 [分组移动记录](evidence/windows-group-move-2026-09-19.md)。

分组重命名见 [重命名记录](evidence/windows-group-rename-2026-09-19.md)，整卡拖拽与统一动效见 [界面验证](evidence/windows-card-drag-2026-09-19.md)。

分组删除与记录保留见 [删除记录](evidence/windows-group-delete-2026-09-19.md)。

文本编辑与富文本降级见 [编辑记录](evidence/windows-text-edit-2026-09-19.md)。

历史列表首尾与翻页按键见 [键盘导航记录](evidence/windows-keyboard-navigation-2026-09-19.md)。
