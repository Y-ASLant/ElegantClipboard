# Windows 排序失败状态回收（2026-09-19）

- 检查 worker 排序命令发现：数据库写入成功后，若列表刷新返回错误，原先会在发送 `Reordered` 事件前退出。现在该失败会作为排序结果返回，并说明顺序已写入、可重启查看。界面收到通用错误时也会清除排序中的锁定和旧拖拽目标。
- 现有排序成功、过期视图拒绝与事务回滚测试继续通过。全 workspace 152 项测试通过；格式检查、严格 Clippy、Release 构建与隔离窗口启动通过。
- 未以真实损坏数据库或故障注入复现刷新失败；该异常路径由控制流审查确认，运行验证覆盖正常排序和编译回归。
