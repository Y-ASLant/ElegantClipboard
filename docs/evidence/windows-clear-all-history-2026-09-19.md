# Windows 全局清空历史验收（2026-09-19）

- “窗口设置”增加独立的“删除全部历史”危险操作。确认区明确说明所有分组、置顶和收藏记录都会删除，设置和自定义分组保留，内容与受管媒体无法恢复，并提示可先导出备份。它与分组栏中保留置顶/收藏的“清理历史”及只处理已加载选择的批量删除保持不同语义。
- 核心在读取全部受管图片和暂存载荷引用成功后删除记录，随后仅清理当前数据目录内不再引用的媒体；读取引用失败时不会先删除数据库行。Windows worker 串行刷新分组计数和当前列表并返回删除数量。GPUI 完成后关闭已删除记录的预览、清空批量选择和移动状态；失败解除等待状态并保留确认区供重试。
- 核心测试覆盖跨默认/自定义分组的置顶、收藏、图片和暂存文件，验证全部记录及受管媒体删除，同时自定义分组和主题设置保留。Windows 后端测试覆盖完成事件、跨分组删除和列表刷新。全工作区 172 项测试通过（核心 134、Windows 后端 33、应用状态/参数 5）；格式检查、严格 Clippy 及 Windows Debug/Release 构建通过且无编译警告。
- 真实 GPUI 窗口使用 `target/qa/clear-all-history-ui-20260919-v1/` 隔离数据目录并关闭采集。截图确认 danger 入口、完整警告和独立确认按钮；执行后 SQLite 记录数从 2 降为 0，`Keep Group` 自定义分组与浅色主题设置仍存在，窗口显示空列表和“已删除全部 2 条历史，设置和分组已保留”。强确认使用独立主内容页，系统最小尺寸请求后的窗口外框为 436×528，警告及两个操作按钮完整可见，下面的历史工具栏不再因确认区而压缩重叠。截图位于忽略目录中的 `clear-all-confirmation.png`、`clear-all-confirmation-min-window.png` 与 `clear-all-complete.png`，测试没有触碰用户数据或系统剪贴板。
- 重新生成 Windows x64 独立 ZIP，SHA-256 为 `fea58e3876a4b4ff7b893299d323ec81db8b22c81b6725bae127eb8250c89b27`；包内 exe 与 Release exe 的 SHA-256 均为 `f4b4b211052894b60e9a1cac9e7df847b6ae0fe821d0ceffacb7b72dbcf40b7f`。解压到 `target/qa/clear-all-history-final-package-84cd60f5735e440287c6d89ca5ad2d7a/` 后运行隔离窗口冒烟，退出码为 0。
