# Windows 历史卡片持续边缘滚动验收（2026-09-19）

- 文本、网址、HTML、RTF、图片和文件卡片继续使用整卡拖拽及 SQLite 持久化排序。历史列表与分组栏现共用 32 px 边缘区、60 ms 基础节拍和统一停止规则；历史列表每两拍移动一行，避免 184 px 行高导致滚动过快。
- 历史列表的边缘滚动改为拖拽生命周期任务，指针停住后仍会继续推进。虚拟列表程序化滚动不会重新发送 `drag_move`，因此任务同时根据可见跨度更新边缘落点，停住后直接放下仍可提交排序。Esc、放下、失效拖动及后台确认都会清理方向和落点状态。
- 使用隔离数据库 `target/qa/card-edge-stationary-20260919` 和真实 Windows 鼠标，将首项拖到列表底边后保持指针静止 1.5 秒再放下。可见列表从第 1 项持续滚到末尾，SQLite 顺序由 `[7,6,5,8,4,3,2,1]` 变为 `[6,5,8,4,3,2,1,7]`，窗口显示“顺序已保存”。截图保存在 Git 忽略目录中的 `at-edge.png`、`after-stationary-hold.png` 与 `after-drop.png`；测试未读写系统剪贴板。
- `cargo test --workspace --locked --quiet` 共 173 项通过（核心 134、Windows 后端 33、应用 6）；应用测试新增空列表、上下边界和可见跨度落点计算。严格 Clippy、格式检查、Windows Release 构建与隔离目录窗口冒烟通过。
