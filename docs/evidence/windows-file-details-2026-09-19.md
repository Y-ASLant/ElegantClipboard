# Windows 文件详情预览验收（2026-09-19）

- 核心层新增中立的文件详情快照，保留记录中的原始顺序和路径，并返回解析后的实际路径、存在状态、文件夹标记、普通文件字节数、受管备份标记及元数据错误。Windows worker 在后台读取快照；GPUI 渲染过程不访问文件系统。
- 受管备份仍沿用原有边界检查：候选必须可规范化为当前数据目录 `staged/` 的直接子文件。源路径有效时优先使用原文件；源路径失效且合法备份存在时，详情同时显示原始路径与备份副本路径。普通失效路径显示主题 danger 状态，复制、定位和另存为仍在执行时重新解析记录。
- 文件预览改为独立滚动的卡片列表，摘要统计总项数、文件夹、失效项、无法读取项和备份副本。每项显示名称、状态与原始路径；普通文件显示格式化大小。底部继续复用图片/文件预览的另存、资源管理器、路径复制和文件复制操作区。
- 全工作区 168 项测试通过（核心 132、Windows 后端 31、应用状态/参数 5），包含有效文件元数据与受管目录内/外备份回退；格式检查、严格 Clippy、GPUI 全目标检查及 Windows Debug/Release 构建通过且无编译警告。
- 真实 GPUI 窗口使用 `target/qa/file-details-ui-20260919-v1/` 隔离数据目录并关闭采集。576×768 截图确认 10 B 文件、文件夹、17 B 备份副本和失效路径的详情及摘要；系统最小尺寸请求后的 436×528 截图确认详情可滚动，四个操作按钮均在窗口边界内并正常换行。截图位于忽略目录中的 `file-details.png` 与 `file-details-min-window.png`，测试没有调用复制、粘贴、资源管理器或另存操作。
- 重新生成 Windows x64 独立 ZIP，SHA-256 为 `a6bc666b2c5bf34638f8218f70d6bef950455d52ea682f7bcf482272217c150c`；包内 exe 与 Release exe 的 SHA-256 均为 `201a6d073d7ea9ad8dba05092a83a0d5c266e16ff26ba4f68e93249cbf5cc980`。解压到 `target/qa/file-details-package-31c45a216ba04ad3b63f8d267f6eba4f/` 后运行隔离窗口冒烟，退出码为 0。
