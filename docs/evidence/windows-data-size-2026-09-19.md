# Windows 数据占用与目录入口（2026-09-19）

- 展开“窗口设置”时异步统计当前实例的数据占用，手动“刷新”使用相同后台命令。统计包含 `clipboard.db`、`clipboard.db-wal`、`clipboard.db-shm`，以及 `images/` 和 `staged/` 下的普通文件；分别返回字节数、图片数、暂存文件数与合计，不将数据目录中的其他文件计入。
- 目录遍历位于现有历史后台线程，不阻塞 GPUI 渲染；符号链接被跳过，目录项或元数据读取失败会返回错误，计数和字节累加使用溢出检查。“打开目录”使用从 `SystemRoot` 解析的 `explorer.exe` 和独立路径参数，不拼接 shell 命令。
- 单元测试覆盖数据库主文件/WAL/缺失 SHM、嵌套图片、暂存载荷、无关文件排除，以及 Explorer 程序和数据目录参数边界。全 workspace 168 项测试通过（核心 132、Windows 后端 31、应用状态/参数 5）；严格 Clippy、格式检查和 Debug 构建通过。
- 真实 GPUI 窗口使用 `target/qa/data-size-ui-20260919-v1/` 隔离数据目录并关闭采集。最终截图中的统计为数据库 639.1 KiB、图片 0 个、暂存 0 个，手动刷新后按钮恢复可用；窗口缩到系统实际边框尺寸 436×528 时，详情文本完整显示，“刷新”和“打开目录”均位于窗口边界内。截图为忽略目录中的 `data-size-min-window.png`。本次没有点击“打开目录”，未启动 Explorer 或改写剪贴板。
