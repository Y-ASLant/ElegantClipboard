# Windows 当前分组历史清理验收（2026-09-19）

- 核心层复用原仓储的“保留置顶与收藏”语义，仅清理当前分组；删除后移除无其他记录引用的受管理图片。Windows worker 校验分组与列表代次，成功后刷新分组计数与当前列表，再发完成结果。
- 主窗口分组栏提供“清理历史”及二次确认，说明搜索和收藏筛选不影响范围、不可撤销，并提示可先导出备份。确认期间禁用重复提交与切组。
- 核心测试覆盖默认/自定义分组隔离、置顶与收藏保留及图片文件清理；平台测试覆盖搜索与收藏筛选下的清理范围、成功结果和过期列表请求拒绝。全 workspace 139 项测试通过，Clippy `-D warnings` 通过。
- 使用 `target/qa/clear-history-ui/` 隔离数据目录并禁用采集打开真实 GPUI 窗口。8 条记录中设 1 条置顶、1 条收藏；点击清理、检查确认文案并确认后，SQLite 只剩这两条，窗口显示 2 条及“已清理 6 条”反馈。截图位于忽略目录，未改动默认用户数据目录或交互桌面剪贴板。
- Windows release 构建、`--smoke-test --data-dir target/qa/clear-history-release-smoke` 与 fmt 检查通过。
