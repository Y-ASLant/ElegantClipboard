# Windows 历史卡片排序动效验收（2026-09-19）

- 文本、网址、HTML、RTF、图片和文件记录沿用整卡拖拽；目标上/下半区决定插入位置，置顶与普通记录分别排序。拖动浮层、落点线和卡片使用同一 Kit 主题。
- 排序成功后，将原列表 ID 顺序与后台快照对比，所有换位的已加载卡片从原位置滑入新位置。过渡共用 180 ms ease-out，`reduce_motion` 开启时不播放；列表成员变化时不播放不准确的位移动画。卡片标题与浮层统一显示“置顶 · 类型”。
- `cargo test --workspace --locked`：149 项通过（122 核心、22 Windows 后端、5 应用），新增测试覆盖被拖动和被挤动卡片的位移计算。严格 Clippy、格式、Windows Debug/Release 构建及 Release 隔离启动冒烟通过。
- 使用隔离数据库和真实鼠标，将首张卡片拖到第二张后半区，SQLite 顺序由 `[7,6,5,8,4,3,2,1]` 变为 `[6,7,5,8,4,3,2,1]`；完成截图保存在忽略目录 `target/qa/card-drag/after.png`。
